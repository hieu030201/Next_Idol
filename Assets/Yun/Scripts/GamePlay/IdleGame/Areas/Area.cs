using UnityEngine;
using Yun.Scripts.Cores;

namespace Yun.Scripts.GamePlay.IdleGame.Areas
{
    public class Area : YunBehaviour
    {
        [SerializeField] public GameObject defaultRoomsContainer;
        [SerializeField] public GameObject functionRoomsContainer;
        [SerializeField] public GameObject battleContainer;
        // Start is called before the first frame update
        protected override void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
